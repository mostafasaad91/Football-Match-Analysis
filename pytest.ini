[pytest]
testpaths = tests
markers =
    slow: generates a whole match package (charts, PDF and Word), which takes
        minutes rather than seconds. Run with `-m "not slow"` to skip them.
    collecting: exercises the shipped default for reference xG -- collect the
        shot map, publish our own model -- rather than the publishing mode the
        other reference tests set.
